parameters:
- name: bus_num
  type:
    name: actions.type.Number
- name: bus_dir
  type:
    name: cardinal_direction
trainingPhrases:
- when will the ($bus_num '14' auto=true) get here?
- 88 ($bus_dir 'west' auto=false)
- I am looking for the ($bus_num '30' auto=true) going ($bus_dir 'south' auto=true)
- bus ($bus_num '51' auto=true)
- I'm headed ($bus_dir 'east' auto=true)
- the ($bus_num '71' auto=true)
- when is the next ($bus_num '147' auto=false) bus coming?
- I want to get on the X($bus_num '11' auto=false)
- ($bus_num '81' auto=false)W($bus_dir ' north' auto=false)
- ($bus_dir 'westbound ' auto=false)($bus_num '201' auto=false)
